<Controls:MetroWindow x:Class="PDF_EVERYTHING.MainWindow"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                      xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
                      mc:Ignorable="d"
                      Title="PDF Everything ToolBox" Height="450" Width="800"
                      ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    <Controls:MetroWindow.Resources>
        <Style x:Key="HoverButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2E2E2E"/>
            <!-- Gray-Black color -->
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFCCCC"/>
                    <!-- Light Red color -->
                    <Setter Property="Foreground" Value="Black"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(Button.RenderTransform).(ScaleTransform.ScaleX)"
                                                 To="1.1" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="(Button.RenderTransform).(ScaleTransform.ScaleY)"
                                                 To="1.1" Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(Button.RenderTransform).(ScaleTransform.ScaleX)"
                                                 To="1" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="(Button.RenderTransform).(ScaleTransform.ScaleY)"
                                                 To="1" Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Controls:MetroWindow.Resources>
    <Grid Margin="0,0,0,-6">
        <TextBlock Text="PDF Everything ToolBox"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Top"
                   FontSize="24" Margin="0,52,0,0" FontFamily="Cascadia Code"/>
        <Button Content="Merge PDF" HorizontalAlignment="Left" Margin="57,178,0,0" VerticalAlignment="Top" Click="Button_Click" Width="140" Height="77" Style="{StaticResource HoverButtonStyle}" FontSize="14"/>
        <Label Content="What do you want to do?" HorizontalAlignment="Left" Margin="57,130,0,0" VerticalAlignment="Top" Width="158" Foreground="White" FontWeight="Bold" Background="#FF771E1E"/>
        <Button Content="Compress PDF" HorizontalAlignment="Left" Margin="228,178,0,0" VerticalAlignment="Top" Click="Button_Click_1" Height="77" Width="140" Style="{StaticResource HoverButtonStyle}" FontSize="14"/>
        <Button Content="Convert PDF To..." HorizontalAlignment="Left" Margin="400,178,0,0" VerticalAlignment="Top" Height="77" Width="140" Click="Button_Click_2" Style="{StaticResource HoverButtonStyle}" FontSize="14"/>
        <Button Content="Spilt PDF" HorizontalAlignment="Left" Margin="575,178,0,0" VerticalAlignment="Top" Height="77" Width="140" Click="Button_Click_3" Style="{StaticResource HoverButtonStyle}" FontSize="14"/>
        <Image HorizontalAlignment="Left" Height="100" Margin="615,16,0,0" VerticalAlignment="Top" Width="100" Source="/toolbox.png"/>
    </Grid>
</Controls:MetroWindow>


